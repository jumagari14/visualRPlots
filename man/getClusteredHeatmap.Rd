% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualPlotFunctions.R
\name{getClusteredHeatmap}
\alias{getClusteredHeatmap}
\title{Create Clustered Heatmap}
\usage{
getClusteredHeatmap(
  inFile,
  figureName = "clusteredHeatmap",
  annotationColumns = NULL,
  annotationColors = NULL,
  showColNames = FALSE,
  showRowNames = FALSE,
  clusterCols = TRUE,
  clusterRows = TRUE,
  cellColors = c("#1E90FF", "#FFFFE0", "#DC143C"),
  scaleRows = FALSE,
  showAnnotColNames = FALSE,
  figureWidth = 1125,
  figureHeight = 625,
  dpi = 150,
  filename = NULL
)
}
\arguments{
\item{inFile}{A numeric matrix/data.frame or a character path to a tab-delimited file
(first column used as row names).}

\item{figureName}{Character. Base name used when saving the figure (default: "clusteredHeatmap").}

\item{annotationColumns}{Optional. Either a data.frame with rownames matching the
columns of the matrix, or a path to a tab-delimited file containing annotation
columns (header present, rownames in first column).}

\item{annotationColors}{Optional. Either a list compatible with pheatmap's
\code{annotation_colors} (named lists), or a path to a tab-delimited file with
three columns: group, name, color (used to construct the named vectors).}

\item{showColNames}{Logical. Show column names on the heatmap (default: FALSE).}

\item{showRowNames}{Logical. Show row names on the heatmap (default: FALSE).}

\item{clusterCols}{Logical. Whether to cluster columns (default: TRUE).}

\item{clusterRows}{Logical. Whether to cluster rows (default: TRUE).}

\item{cellColors}{Character vector of colors to interpolate for the heatmap
palette (default: c("#1E90FF","#FFFFE0","#DC143C")).}

\item{scaleRows}{Logical. If TRUE, scale rows (default: FALSE).}

\item{showAnnotColNames}{Logical. Show annotation column names (default: FALSE).}

\item{figureWidth}{Numeric. Width in pixels when saving (default: 1125).}

\item{figureHeight}{Numeric. Height in pixels when saving (default: 625).}

\item{dpi}{Numeric. Resolution used when saving (default: 150).}

\item{filename}{Optional. If provided, save the heatmap to this file. If NULL,
a file named paste0(figureName, ".png") will be written when saving.}
}
\value{
A list invisibly containing pheatmap output (gtable and components).
}
\description{
Builds a clustered heatmap using pheatmap. The input can be either a numeric
matrix/data.frame or a path to a tab-delimited file with row names in the
first column. Optional annotation columns and annotation color mappings are
supported.
}
\examples{


# From a matrix
\dontrun{
m <- matrix(rnorm(100), nrow=10)
rownames(m) <- paste0("gene", 1:10)
hm <- getClusteredHeatmap(m)
}

}
