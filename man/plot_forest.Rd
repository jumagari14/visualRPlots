% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualPlotFunctions.R
\name{plot_forest}
\alias{plot_forest}
\title{Create Forest Plot from Linear Models}
\usage{
plot_forest(
  model_list,
  adjust_pvals = FALSE,
  pval_cutoff = 0.05,
  remove_intercept = TRUE
)
}
\arguments{
\item{model_list}{A list of linear model objects (lm, glm, etc.) or a data frame
of tidied model results}

\item{adjust_pvals}{Logical. Adjust p-values using Bonferroni correction (default: FALSE)}

\item{pval_cutoff}{Numeric. P-value cutoff for significance (default: 0.05)}

\item{remove_intercept}{Logical. Remove intercept term from plot (default: TRUE)}
}
\value{
A ggplot2 object
}
\description{
Creates a forest plot from linear model results, useful for visualizing
effect sizes and confidence intervals across multiple models or traits.
}
\examples{
\dontrun{
models <- list(model1 = lm(y ~ x1, data = df1),
               model2 = lm(y ~ x2, data = df2))
plot_forest(models, adjust_pvals = TRUE)
}
}
